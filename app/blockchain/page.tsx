import { DomainProjects } from '@/components/domain-projects';

export default function BlockchainPage() {
  return (
    <DomainProjects
      domain="blockchain"
      heading="BLOCKCHAIN"
      subheading="Smart contracts and on-chain infrastructure. Categories are admin-editable — filter with the chips below."
    />
  );
}
